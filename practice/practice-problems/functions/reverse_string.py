# Practice problem 
# Python function that reverse a string

def reverse_strng(digits):
    for i in range(len(digits)):
        last_item = digits.pop()
        print(ligits.insert(i, last_item))

lst = [1, 2, 3, 4, 5, 6, 7]
reverse_strng(lst)
